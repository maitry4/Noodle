// ignore: dangling_library_doc_comments
import 'package:flutter/material.dart';

/// The floating overlay with buttons based on what's happening. it will also have a small settings gear that takes you to modify options page.
class FloatingCharacter extends StatelessWidget {
  const FloatingCharacter({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}