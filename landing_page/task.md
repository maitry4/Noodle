# Tasks

- `[/]` Update `globals.css` with Noodle's colors and custom font.
- `[ ]` Update `layout.tsx` to load and apply `Quicksand` or `Fredoka`.
- `[ ]` Delete `IntroOverlay.tsx` and `SocialProof.tsx`.
- `[ ]` Update `page.tsx` with the two-phase layout and animation over the native video.
- `[ ]` Update `Hero.tsx` with minimal, clean code and Noodle's colors.
- `[ ]` Update `FloatingComplaint.tsx` to use Noodle's olive green color and cute styling.
- `[ ]` Verify changes locally.
